import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      Bu proje Hyper Teknoloji tarafından gönderilmiş olup, teknik değerlendirme
      çalışmasıdır.
    </footer>
  );
};

export default Footer;
